name: Deploy Apps

on:
  workflow_run:
    workflows: ["Random"]
    types:
      - completed
    branches:
      - main
      - 'srimanthd/*'

jobs:
  deploy-to-dev:
    runs-on: ubuntu-latest
    steps:
      - name: echo
        shell: bash
        run: |
          echo "Hello world"
